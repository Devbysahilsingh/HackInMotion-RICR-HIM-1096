You are continuing the HackInMotion 2026 project in a NEW CONTEXT WINDOW.

IMPORTANT: Treat the repository itself and its documentation as the source of truth. Do not assume anything from the previous conversation that is not verified from the current repository.

============================================================
PROJECT CONTINUATION STATE
============================================================

HackInMotion 2026
Project: Smart Farm Decision Support System

The approved architecture and complete planning corpus already exist in the repository.

P0-3 — Development Foundation — is COMPLETE.

P0-3 included:

- Repository tooling
- Backend foundation
- Web foundation
- Shared structure
- ESLint
- Prettier
- TypeScript configuration
- EditorConfig
- .gitignore
- Gitleaks
- Pre-commit secret scanning
- Environment validation
- Express backend
- /healthz
- Security/error-handling foundation
- React + Vite + TypeScript + Tailwind web scaffold
- Documentation
- Verification
- Security audit

P0-3 was fully tested and security-hardened.

The final verification included:

- Backend boot
- /healthz
- Environment validation
- Web production build
- Browser runtime verification
- ESLint
- Prettier
- TypeScript
- Gitleaks 8.30.1
- Gitleaks clean scan
- Fake-secret detection
- Pre-commit blocking
- Secret audit
- Git staging audit
- Documentation verification

Two real secret-scanning weaknesses were discovered during verification and fixed:

1. Unquoted API_KEY=value style assignments were not detected.
2. Word-boundary matching failed for snake_case names such as AWS_ACCESS_KEY_ID.

Both were fixed and regression-tested.

No real credentials were used.

No real API keys belong in the repository.

============================================================
FIRST: VERIFY CURRENT STATE
============================================================

Before doing ANY implementation:

Read and verify:

- CLAUDE.md
- README.md
- docs/development/MASTER-TODO.md
- docs/FINAL-PLAN-SPEC.md
- docs/development/implementation-log.md
- docs/requirements-traceability.md
- relevant docs/ml/*
- relevant architecture/decision documents
- relevant ADRs

Then inspect:

git status
git log --oneline --all
git ls-files
git diff --stat
git branch --show-current

Do NOT assume that P0-3 Git commits exist.

If the current repository shows P0-3 commits already exist, record that fact.

If they do NOT exist, do not recreate or alter them automatically. The working tree/documentation state is still the source of truth for implementation continuity.

Most importantly:

P0-3 implementation itself is COMPLETE.

Do not redo P0-3.

Do not rebuild the scaffold.

Do not repeat P0-3 unless a genuine regression is discovered.

============================================================
CURRENT PHASE
============================================================

We are now starting:

P0-4 — Dataset Acquisition

TITLE:
Dataset acquisition — download source datasets, verify integrity, capture licenses, produce inventory report

PRIORITY:
P0

STATUS:
APPROVED TO IMPLEMENT

This is the next implementation phase.

Do NOT recommend another TODO.

Do NOT implement P0-5 yet.

Do NOT implement backend auth yet.

Do NOT implement ML training yet.

Complete P0-4 first.

============================================================
WHY P0-4 IS NEXT
============================================================

P0-4 is the longest-lead and highest-uncertainty ML dependency.

The ML pipeline is:

P0-4 Dataset Acquisition
        ↓
P0-5 Dataset Audit
        ↓
P0-6 Dataset Preparation
        ↓
Training / Evaluation
        ↓
ML Service
        ↓
Crop Health Feature

Dataset acquisition must therefore happen early.

The actual datasets determine:

- available crops
- class distribution
- disease classes
- image quality
- licensing
- duplication
- feasibility on RTX 2050 4GB
- final ML scope
- whether specialized crop models are justified

Do NOT assume the final ML classes before P0-5 audit.

============================================================
P0-4 APPROVED SCOPE
============================================================

Implement ONLY dataset acquisition and inventory.

The objective is:

Download
→ verify
→ document provenance
→ capture license
→ checksum
→ inventory
→ prepare for P0-5 audit

Do NOT perform:

- class census
- deduplication
- perceptual hashing
- detailed quality audit
- train/validation/test split
- preprocessing
- augmentation
- model training
- model evaluation
- PyTorch training environment
- ML service
- backend crop-health implementation
- Gemini integration
- mobile implementation

Those belong to later TODOs.

============================================================
DATASETS
============================================================

The currently planned acquisition set is:

1. PlantVillage
   - Relevant tomato/potato/maize subsets
   - Use credible source/mirror
   - Record exact provenance

2. PlantDoc
   - Field-condition crop images
   - Record exact provenance and license

3. Mendeley chilli dataset(s)
   - Relevant chilli disease data
   - Record exact source and license

4. SAR-CLD-2024 cotton
   - Cotton disease/leaf dataset
   - Record exact source and license

5. Paddy Doctor
   - Rice dataset
   - Kaggle authentication may be required

IMPORTANT:

Do not blindly assume every planned dataset is usable.

For each source:

- Verify it exists
- Verify accessibility
- Verify license
- Verify source provenance
- Verify download integrity
- Verify archive structure
- Record actual downloaded content

If a source is unavailable, unlicensed, broken, suspicious, or unsuitable:

DO NOT silently replace it.

Report:

SOURCE
→ PROBLEM
→ IMPACT
→ ALTERNATIVES
→ RECOMMENDATION

Then use professional judgment for a safe replacement only if it remains within the approved project scope and licensing requirements.

============================================================
KAGGLE / PADDY DOCTOR
============================================================

Paddy Doctor is currently the potentially blocked source.

DO NOT ask me to paste Kaggle credentials.

Look for Kaggle authentication through the normal secure local mechanism, such as:

~/.kaggle/kaggle.json

or the appropriate Windows equivalent.

If credentials are present:

Use them securely.

Never:

- print them
- copy them into the repository
- commit them
- log them
- include them in documentation

If credentials are absent:

Do NOT block the other four datasets.

Proceed with all available datasets.

Clearly mark Paddy Doctor as:

BLOCKED — Kaggle credentials unavailable

Do not fake the rice dataset.

Do not fabricate a replacement without documenting it.

============================================================
DOWNLOAD IMPLEMENTATION
============================================================

Create a reproducible, idempotent acquisition mechanism.

Recommended:

scripts/ml/download-datasets.py

and only the minimum supporting files required.

The script should support:

- resumable downloads where practical
- skip-if-already-valid
- retry handling
- clear progress
- HTTPS
- checksum verification
- archive extraction
- safe extraction
- idempotent re-run

Do not add PyTorch or heavy ML dependencies.

P0-4 is DATA ACQUISITION ONLY.

Prefer Python standard library + requests or another minimal stable HTTP dependency.

============================================================
SECURE ARCHIVE EXTRACTION
============================================================

Third-party archives must be treated as untrusted input.

Prevent zip-slip/path traversal.

Reject archive entries that resolve outside the intended dataset directory.

Never execute downloaded content.

Only extract expected data.

============================================================
INTEGRITY VERIFICATION
============================================================

For each downloaded dataset:

Record:

- source
- URL
- download date
- archive/file name
- size
- checksum
- extraction status
- extracted file count
- image count where determinable
- license status
- license source
- provenance notes
- errors/warnings

Perform sample image decoding.

Do not hide corrupt files.

Report actual numbers.

If published counts differ from actual counts:

record the discrepancy.

============================================================
CHECKSUM MANIFEST
============================================================

Create:

datasets/manifest-raw.json

or the approved equivalent.

It must contain reproducible integrity information.

Do NOT commit large raw datasets.

Raw datasets must remain gitignored.

============================================================
LICENSE COMPLIANCE
============================================================

Capture the relevant license text into:

datasets/licenses/

Only small license/provenance files should be committed.

For each dataset record:

- license name
- license URL/source
- usage restrictions
- attribution requirements
- whether commercial use is permitted if known
- whether redistribution is permitted if relevant
- any ambiguity

If licensing cannot be established:

FLAG IT.

Do not silently assume "publicly available = freely usable".

============================================================
DOCUMENTATION
============================================================

Update:

docs/ml/dataset-research.md

with the actual acquisition results.

Also update:

docs/development/implementation-log.md

and:

docs/development/MASTER-TODO.md

ONLY after P0-4 is genuinely complete.

Record:

- datasets acquired
- datasets unavailable
- dataset sizes
- checksums
- licenses
- provenance
- errors
- deviations
- security checks
- verification results
- Paddy Doctor/Kaggle status

============================================================
GIT SAFETY
============================================================

Raw datasets MUST NOT be committed.

Verify:

git status

and ensure:

datasets/raw/
large archives
model artifacts
temporary files
credentials
.env

are not commit candidates.

Only commit:

- acquisition scripts
- small manifests
- licenses
- documentation
- configuration required for reproducibility

Continue using the existing secret-scanning protections.

============================================================
IMPLEMENTATION LOOP
============================================================

Work in this exact loop:

PLAN SMALL STEP
→ IMPLEMENT
→ CHECK
→ TEST
→ FIX
→ RE-CHECK
→ VERIFY
→ DOCUMENT

Do not perform a huge unverified download/processing operation and only inspect at the end.

For every meaningful failure:

1. Diagnose
2. Fix
3. Re-run
4. Verify
5. Continue

Do not silently ignore failures.

============================================================
USE MULTIPLE AGENTS / PARALLEL RESEARCH
============================================================

You may use multiple agents/subtasks where useful.

For example:

Agent 1:
Dataset source/provenance research

Agent 2:
License verification

Agent 3:
Download/integrity research

Agent 4:
ML dataset feasibility research

BUT:

Do not duplicate downloads unnecessarily.

Do not allow parallel agents to modify the same files without coordination.

The final repository state must remain consistent.

============================================================
HUMAN DECISION RULE
============================================================

Do not ask me about routine technical decisions.

Use professional engineering judgment for:

- download implementation
- retry strategy
- checksum algorithm
- folder naming
- archive extraction
- manifest format
- documentation structure
- small dependency choices

STOP and ask me only if there is a major decision involving:

- Dataset replacement
- Licensing uncertainty with material consequences
- Major change in ML scope
- Removing a required crop
- Changing the approved architecture
- Significant cost
- Security risk
- Problem-statement compliance

When asking:

Explain:

DECISION
→ OPTIONS
→ RECOMMENDATION
→ TRADE-OFFS
→ IMPACT

============================================================
P0-4 COMPLETION GATE
============================================================

Do NOT mark P0-4 complete until:

[ ] All accessible approved datasets attempted
[ ] Paddy Doctor status explicitly resolved
[ ] Downloads verified
[ ] Checksums recorded
[ ] Archives safely extracted
[ ] Sample images decode successfully
[ ] File counts recorded
[ ] Licensing captured
[ ] Provenance recorded
[ ] Raw datasets gitignored
[ ] No credentials in repository
[ ] No large dataset accidentally staged
[ ] Acquisition script is reproducible
[ ] Re-running is idempotent
[ ] Documentation updated
[ ] MASTER-TODO updated
[ ] Implementation log updated
[ ] Security scan passes
[ ] Repository audit passes

============================================================
IMPORTANT ML RULE
============================================================

Do NOT decide the final crop/model scope during P0-4.

P0-4 acquires evidence.

P0-5 will audit the evidence.

P0-5 will determine:

- actual class counts
- class imbalance
- image quality
- duplication
- crop viability
- licensing confidence
- specialized vs general model feasibility
- final ML class scope

Do not prematurely optimize the model before the dataset audit.

============================================================
AFTER P0-4
============================================================

Once P0-4 is completely verified:

1. Update MASTER-TODO.md.
2. Update implementation-log.md.
3. Update dataset research documentation.
4. Record important decisions/limitations.
5. Run final security audit.
6. Run final repository audit.
7. Show exactly what was implemented.
8. Show dataset acquisition results.
9. Show verification results.
10. Show any failures and fixes.
11. Show Paddy Doctor/Kaggle status.
12. Show exact files changed.
13. Give me the recommended Git commit strategy.

DO NOT create the Git commit automatically.

Then recommend ONLY ONE next TODO.

Based on the current plan, it will most likely be:

P0-5 — Dataset Audit

But do not blindly assume that.

Re-evaluate the dependency graph after P0-4 and recommend the strongest next TODO.

STOP and wait for my approval.

============================================================
MOST IMPORTANT RULE
============================================================

Do not sacrifice correctness for speed.

Do not sacrifice security for convenience.

Do not sacrifice licensing for dataset quantity.

Do not sacrifice ML quality for having more crops.

Do not invent data.

Do not fake verification.

Do not silently ignore unavailable sources.

The objective is not to download the largest amount of data.

The objective is to build a legally usable, reproducible, high-quality dataset foundation for the custom crop-health AI that can realistically run on our RTX 2050 4GB and support the final HackInMotion product.

START P0-4 NOW.