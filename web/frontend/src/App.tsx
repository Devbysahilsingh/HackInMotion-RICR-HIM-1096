/**
 * Scaffold placeholder.
 *
 * Routing, i18n, React Query and the design system are introduced by the web
 * TODOs (docs/frontend/architecture.md). This file exists only to prove the
 * toolchain renders, and will be replaced by the router shell.
 */
export default function App() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-50 p-6">
      <div className="max-w-md text-center">
        <h1 className="text-2xl font-semibold text-slate-900">
          Smart Farm Decision Support System
        </h1>
        <p className="mt-2 text-sm text-slate-600">
          Web scaffold running. Application shell arrives with the next TODO.
        </p>
      </div>
    </main>
  );
}
