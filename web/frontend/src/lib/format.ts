/**
 * Locale-aware formatters — re-exported from `shared/client/format.ts`.
 *
 * Moved out of this app when the mobile client landed: ₹ grouping, the em-dash
 * empty value and the relative-age key mapping are product decisions, and both
 * surfaces have to make them the same way.
 */
export * from '@shared/client/format';
