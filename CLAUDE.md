# CLAUDE.md — Operating Manual (Smart Farm Decision Support System · HackInMotion 2026 · HIM-1096)

**Read this before any work. `docs/FINAL-PLAN-SPEC.md` is the single source of truth for intent; `docs/development/implementation-log.md` is the source of truth for what actually exists and has been verified; this file is the enforcement layer.**

**Current phase: IMPLEMENTATION — controlled, one TODO at a time.** The user recommends/approves each TODO before it is built. Never chain TODOs autonomously: implement exactly the approved TODO, verify it, report, then stop and wait. If an unexpected prerequisite appears, stop and explain rather than absorbing it silently.

**Completed:** P0-3 development foundation · P0-4 dataset acquisition · P0-5 dataset audit · P0-5b rice healthy acquisition · P0-6/P0-6b dataset preparation + pre-training gates (no training) · **P1-1..P1-8 backend foundation** (Express hardening, 14 models + index assertions, full auth lifecycle with refresh-family reuse detection, ownership layer, Farms + Crops CRUD, sourced crop registry with versioned seed, pure stage engine) · **P2-1..P2-9 data pipelines & engines** (weather ingestion with OWM fallback, weather-risk engine, FAO-56 irrigation engine R1–R14 + ledger API, data.gov.in normalizer + nightly job, market signal + endpoints, feed composer + `/dashboard`, fertilizer guidance, crop-recommendation engine, in-process scheduler) · **P3-1..P3-8 crop health chain** (upload pipeline + ST-30, ml-service FastAPI + ONNX harness with stub model, Gemini + OpenRouter under one normalized contract, rule-based symptom engine, health conductor with confidence gating/tier-down/image-hash cache, engine-derived severity, community aggregation + alerts + ST-20) plus the **sourced disease KB** it depended on (35 codes, 408 English strings, TNAU/ICAR/NIPHM). Decisions: ADR-021, ADR-022, ADR-023, **ADR-024**. **PHASE 1 COMPLETE (approved 2026-08-13). PHASE 2 COMPLETE (approved 2026-08-13). PHASE 3 CODE COMPLETE (2026-08-13, awaiting approval)** — 1,203 backend tests + 141 ml-service pytest green; **STATUS CORRECTED 2026-08-14 — this list stopped at Phase 3 and is six phases behind. Since then: P4 ML training + ONNX export (`model-v1.0` shipped), P5 web client, P6 Expo Android client, P7 security hardening audit, P8 deploy prep (deliberately not executed until the qualifier result), P9 submission deliverables, plus the offline irrigation write-sync feature. Current gates on `main`: 41 mounted routes, ESLint 0 errors, web + mobile typechecks clean, Prettier clean, i18n 1493 keys / 0 missing in hi, 0 hardcoded UI strings, web 131/131, ST-10 138/138, ST-11 IDOR 42/42. Full current state: `docs/development/submission-audit.md` §0.** ST-01..05/ST-10/ST-20/ST-30/ST-50/ST-70 and RES-01/02/03/07 green; 8-combination router matrix green. **Outstanding external items:** Render deploy (owner A) · data.gov.in key + resource id (OD-5) · demo states + IMD climate normals (OD-7) · wheat/soybean fertilizer verification · LIMITED crop roster approval · **disease-KB Hindi 0/408 (blocks cotton's ADR-021 ship gate)** · ~~no live Gemini/OpenRouter/Cloudinary/ml-service call has been made — all four credentials unset~~ **CLOSED 2026-08-14: all four are configured and have answered live** — ml-service 200 (`OnnxPredictor`, `model-v1.0`), Gemini 200 in 5.87 s, OpenRouter 200 in 6.02 s at zero cost, Cloudinary URL persisted; evidence in `docs/development/submission-audit.md` §3c · ~~calibrated τ/τ_healthy await the training run~~ **CLOSED: τ 0.7 / τ_healthy 0.8, temperature 0.586277, shipped in the model manifest.**

**ML status (corrected 2026-08-14):** training and export are **done**. The shipped model is **`model-v1.0`** (`ml-service/model/model-v1.0.onnx`), whose manifest reports `trained: true`, `calibrated: true`, `provisional: false` — 35 classes, temperature 0.586277, τ 0.7 / τ_healthy 0.8. It has been **executed live** on a real field photograph (78 ms; see `docs/development/submission-audit.md` §3c). It is no longer `stub-0.0.0-untrained`, and `datasets/` is no longer untouched. **Read the accuracy honestly:** in-domain test 0.9632 but **field-domain (PlantDoc) 0.1257** — on the one live field photo tried, the model was confidently wrong and the Gemini tier caught it. Never present this model as accurate on farmer-taken photographs.

**How to run what exists:** root `npm install` (installs tooling and enables the pre-commit hook) · `cd backend && npm install && npm run dev` (needs `backend/.env`; copy names from the root `.env.example`) · `cd web/frontend && npm install && npm run dev`. Repo-wide checks: `npm run lint`, `npm run format:check`. Backend: `npm test` (full suite — **1,566** at the last full run; the "1,203" figure here was from Phase 3) · `npm run indexes:build` · `npm run seed:registry [--dry-run]` · `npm run seed:market [--dry-run]` · `npm run jobs -- <jobName> [--json]` · `npm run smoke -- <url>`. ml-service: `cd ml-service && python -m venv .venv && ./.venv/Scripts/pip install -r requirements.txt -r requirements-dev.txt && ./.venv/Scripts/python -m pytest` (**143** at the last full run, 1 known manifest-hash failure; the "141" figure here was from Phase 3). To actually **serve the trained model** to a local backend, set `MODEL_PATH=model/model-v1.0.onnx` and a `SERVICE_KEY` **matching `backend/.env`**, then `./.venv/Scripts/python -m uvicorn app.main:create_app --factory --host 127.0.0.1 --port 7860`; a mismatched key starts cleanly and 401s every call, so the chain silently tiers down to Gemini instead of using the model (see `ml-service/README.md`).

## Project context
India-focused farmer decision-support platform (web + Android). Farmer → Farm → Crop → decisions: irrigate? weather risk? crop sick? fertilize? sell? plant what? Every recommendation personalized (≥3 profile factors), explainable (why-trace), localized (hi/en), resilient (cache-first), and honest (freshness/confidence labels). Full problem statement: FINAL-PLAN-SPEC §2; traceability: docs/requirements-traceability.md.

## Architecture (fixed — do not re-architect)
React+Vite web (Vercel) & React Native+Expo Android — one REST contract → Node20/Express (Render) with pure engines + cron jobs → MongoDB Atlas M0 → FastAPI+ONNX ml-service (internal, X-Service-Key). External: Open-Meteo→OWM, data.gov.in→seed, Gemini→OpenRouter→rules, Cloudinary. (~~Groq(P2)~~ — **never integrated**: web speech-to-text uses the browser's Web Speech API and mobile ships TTS only, so the planned `/voice/transcribe` proxy has no consumer and is not mounted.) Details: docs/architecture/, docs/backend/, docs/frontend/, docs/mobile/.

## Non-negotiable rules
1. **One approved TODO at a time.** No feature work beyond the approved TODO's scope; no autonomous chaining; no final ML training or deployment without its own approval.
2. **Security = feature:** every endpoint: Zod validation + auth + ownership (404 pattern) + rate limit + safe errors. NO backdoors, master passwords, hidden/secret routes, demo auth bypasses, hardcoded credentials, client-only authorization — ever, under any time pressure. Demo runs production security config.
3. **DB-first reads:** request paths never call weather/market providers; jobs ingest with validate-then-cache; failures never overwrite last-known-good.
4. **Registry-driven crops:** `if (cropCode === 'TOMATO')`-style conditionals are banned; all crop knowledge lives in cropRegistry documents.
5. **Engines are pure:** engines/ import nothing above utils/constants; deterministic; fixture-tested; every output carries a trace (R12).
6. **AI perceives, engines decide, KB speaks:** LLMs never author farmer-facing agronomic advice, dosages, or treatment text; Gemini output is schema-validated, registry-closed; uncertain results are never forced into predictions.
7. **No fabricated anything:** no invented metrics, NPK numbers, yields, licenses, API limits, or "demo mode" fake data. Seeded demo data = real historical/labeled data. Failed runs get logged as failed.
8. **i18n:** zero hardcoded user-facing strings; keys+params in DB/API; shared/i18n canonical; agronomic Hindi requires human verification before demo.
9. **Honesty labels:** cached/stale/AI-assisted/estimated content always labeled (● system); severity is engine-assessed, never model-fabricated.
10. **Zero cost:** no paid service may be introduced; free-tier quotas guarded by per-user caps.
11. **Secrets:** env-only; Gitleaks hook; never in code/README/screenshots/logs/client bundles.
12. **Privacy:** community data district-aggregated, consent-gated, structurally PII-free; no analytics trackers; images private per account.

## Conventions
- **Code style:** match surrounding file; TypeScript on clients, JS (or TS if scaffolded so) backend; conventional commits (`feat(scope): …`); comments only for non-obvious constraints. **No Claude co-author or attribution trailer** — every commit carries the repository author alone (this reverses the original instruction, by the project owner's standing direction).
- **API:** /api/v1; envelope {success, data|error{code,messageKey,details}}; codes from docs/api/error-codes.md only; no endpoint without an FR mapping.
- **DB:** schemas/indexes per docs/database/; every owned doc has userId; no new collections without documented justification (see "deliberately NOT collections" list).
- **ML:** all work per docs/ml/ (audit gates → training config-driven → evaluation battery → ship gates → parity-tested ONNX). Major strategy changes (arch swap, class restructure, dataset add/drop) require team approval; routine experiments don't. Metrics only from committed artifacts.
- **Testing:** blocking suites in docs/testing/strategy.md table; feature DoD below; failure-injection flags are routing-only, non-prod.
- **Git:** feature branches → PR → human review (always, for Claude-authored code); main stays demoable; no fake/padding commits.
- **Docs:** update the relevant docs/ file in the same PR as the change it describes; ADR for any decision that alters an existing ADR.

## Definition of Done (per feature)
Implements FR + validation + ownership + safe errors + rate limit (if applicable) + i18n keys en+hi + loading/empty/error/offline states + freshness labels (if data-bearing) + tests per test-matrix + docs updated + security abuse cases considered (docs/security DoD).

## Things Claude must NOT do
Start implementation before the gate · weaken/skip any rule above to save time · add dependencies beyond docs/security/dependency-security.md list without stated justification · invent agronomic facts, translations of agronomic terms without flagging for human verification, or dataset/license claims · create admin surfaces, notification services, or new external dependencies "while at it" · commit datasets, models >LFS threshold, .env, or generated artifacts · optimize for feature count over the P0 core · silently execute the cut-order (announce cuts) · claim anything works that hasn't run.

## Open decisions (check before relying)
OD-1 cotton gate (post-audit) · OD-2 ml-service host (Day-1 latency test) · OD-3 team headcount · ~~OD-4 product name~~ **CLOSED 2026-08-14 — the product is Khetri** (display names and Android identifiers renamed while no APK existed) · OD-5 data.gov.in key · OD-6 Kaggle credentials. Full list: FINAL-PLAN-SPEC §42.
